# CLAUDE.md — دليل الوكيل لمشروع «موكب أمنا الزهراء»

> اقرأ هذا الملف **قبل** تنفيذ أي طلب في هذا المستودع. يحوي كل ما تحتاجه:
> البنية، القرارات المحسومة، القواعد التي لا تُكسر، أوامر التشغيل، ومزالق
> البيئة التي أهدرت وقتاً سابقاً.

---

## ١. المشروع في سطور

تطبيق موبايل لإدارة موكب حسيني: مشتركون ومتبرعون، مبالغ ودفعات، تقارير
قابلة للطباعة، ومنشورات بنمط إنستغرام.

| | |
|---|---|
| **الإطار** | Flutter · معمارية feature-first · Riverpod · go_router |
| **الخلفية** | Supabase (Auth + Postgres + Storage + Realtime + RLS) |
| **التخزين المحلي** | Hive مشفّر AES-256، المفتاح في Keystore/Keychain |
| **المنصّات** | Android (المستهدف الأساسي) · iOS (الكود جاهز، البناء يحتاج macOS) |
| **اللغة** | العربية فقط · RTL · كل النصوص والتعليقات والـcommits بالعربية |
| **المالك** | `aliali121ew-coder` · المستودع `zahraa` |

---

## ٢. أوامر سريعة

```bash
# ⚠️ بعد استنساخ المستودع لأول مرة: اجلب خطوط Amiri
bash tools/fetch_fonts.sh          # لينكس و macOS و CI
powershell -ExecutionPolicy Bypass -File tools\fetch_fonts.ps1   # ويندوز

# تشغيل بالبيانات الحقيقية
flutter run \
  --dart-define=SUPABASE_URL=https://nmpcbyoehmghmietzurs.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=sb_publishable_ev4vJ_FdXww-7Yp_ojV5CA_XDJcrTxW

# تشغيل بلا مفاتيح = وضع البيانات التجريبية الكامل (كل الواجهات تعمل)
flutter run

# بناء APK للتوزيع
flutter build apk --release \
  --target-platform android-arm,android-arm64 \
  --obfuscate --split-debug-info=build/symbols \
  --dart-define=SUPABASE_URL=... --dart-define=SUPABASE_ANON_KEY=...

# الفحص والاختبار — شغّلهما قبل أي commit
flutter analyze && flutter test

# معاينة التصميم كصورة PNG بلا جهاز ولا محاكي
flutter test test/design_preview_test.dart --update-goldens
# الناتج: test/goldens/home_dark.png و home_light.png
```

> **معاينة التصميم أداة مهمة:** لا تعدّل الواجهة ثم تسلّم بلا رؤيتها.
> شغّل المعاينة وافحص الصورة الناتجة فعلياً قبل أن تقول «تم».

---

## ٣. البنية

```
lib/
├── core/
│   ├── config/      app_config.dart — المفاتيح من --dart-define وأسماء صناديق Hive
│   ├── data/        supabase_repository.dart — الأساس المشترك + CachedResult + arabicError
│   ├── providers/   app_providers.dart — كل مزوّدات Riverpod
│   ├── router/      app_router.dart — StatefulShellRoute بخمسة فروع
│   ├── storage/     hive_service.dart (مشفّر) · settings_store.dart
│   ├── theme/       app_colors.dart · app_theme.dart
│   ├── utils/       formatters.dart — Fmt للمبالغ والتواريخ
│   └── widgets/     glass.dart (AppBackground/GlassCard/GoldBorder) · girih_pattern.dart · app_shell.dart
├── features/<الميزة>/
│   ├── data/            المستودع (Repository)
│   └── presentation/    الشاشات
└── shared/
    ├── models/      النماذج + enums.dart + permissions.dart
    └── widgets/     stat_cards.dart · contributor_tile.dart · mawkib_logo.dart
supabase/schema.sql   الجداول + RLS + الدوال + المشغّلات
```

**نمط الميزة الجديدة:** `features/<الاسم>/data/<الاسم>_repository.dart` يرث
`SupabaseRepository`، و `features/<الاسم>/presentation/<الاسم>_page.dart`،
ومزوّداتها تُضاف في `core/providers/app_providers.dart`.

---

## ٤. قواعد لا تُكسر

1. **الأمان في السيرفر لا في التطبيق.** أي صلاحية جديدة تُفرض في RLS داخل
   `supabase/schema.sql` **أولاً**، ثم تُعكس في `shared/models/permissions.dart`
   لإخفاء الأزرار. إخفاء زر ليس حماية.

2. **لا تكسر تكافؤ الصلاحيات.** `permissions.dart` مرآة لسياسات RLS. تعديل
   أحدهما بلا الآخر خطأ.

3. **العضو لا يرى الأسماء إطلاقاً.** أرقامه تأتي من دالة `get_stats()` المجمّعة
   لا من الجداول. لا تُضف مساراً يسرّب أسماء لدور `member`.

4. **التبرع العيني لا يدخل أي مجموع نقدي.** لا في `get_stats` ولا في المشغّلات
   ولا في الواجهة. يُعدّ فقط.

5. **حماية من الـoverflow في كل شاشة:** كل رقم كبير داخل `FittedBox`، وكل نص
   متغيّر له `maxLines` و `TextOverflow.ellipsis`. مقياس خط النظام محدود في
   `app.dart` بين 0.9 و 1.25.

6. **الزجاجي بحساب.** `GlassCard(blur: true)` للكروت **الثابتة** فقط ومحدودة
   العدد. داخل أي قائمة متمرّرة استخدم `blur: false` — التمويه يُعاد حسابه لكل
   عنصر في كل إطار ويقتل الـ60fps.

7. **لا تُودع أسراراً.** `android/keys/` و `android/key.properties` مستثناة في
   `.gitignore`. لا ترفع `.jks` ولا كلمة مرور. مفتاح Supabase العلني استثناء
   مقصود — مصمَّم ليكون مكشوفاً، والحماية في RLS.

8. **صندوق `outbox` مقدّس.** يحوي كتابات لم تُرفع. لا تحذفه عند فشل الفتح —
   `hive_service.dart` يرمي الخطأ عمداً بدل ابتلاعه. أي «إصلاح» يحذفه صامتاً
   يُضيّع عمل المستخدم.

9. **لا `build_runner`.** النماذج تُكتب يدوياً بـ `fromJson`/`toJson`، وHive
   يخزّن خرائط JSON بلا TypeAdapter. توليد الكود يفشل بصمت ويعطّل البناء.

10. **العربية في كل شيء:** الواجهة، التعليقات، رسائل الـcommit، أسماء الأقسام.
    الأخطاء التقنية تُترجم عبر `arabicError()` — لا تعرض `42501` للمستخدم.

---

## ٥. نظام التصميم

### الألوان
مستخرجة **قياساً فعلياً** من بكسلات شعار الموكب — لا تخترع ألواناً جديدة.

| | |
|---|---|
| ذهبي أساسي | `#C6A77B` (`AppColors.gold`) |
| ذهبي ساطع | `#EBD3A8` (`goldBright`) — للنصوص على الداكن |
| ذهبي داكن | `#8A6A33` (`goldDark`) — للنصوص على الفاتح |
| برونزي | `#8D7759` · عنابي `#7D493A` · فيروزي `#3D6D78` |
| سلّم الأخضر | `greenAbyss #04120C` → `green #14512F` → `greenGlow #1B7A4B` |
| الحالات | مسدد `#2E9E6B` · متأخر `#C4553F` · معلّق `#D79A3C` |

### الطباعة — نظام خطّين
- **`AppTheme.displayFamily` = Amiri** (نسخي كلاسيكي): العناوين، أسماء
  الأشخاص، تسميات الكروت، علامة الموكب.
- **`AppTheme.fontFamily` = IBM Plex Sans Arabic**: الأرقام، الأزرار، النصوص
  الصغيرة، الجداول.

> **التباين بين الخطّين هو مصدر الإحساس الاحترافي.** استخدام خط واحد لكل شيء
> يجعل الواجهة تبدو قالباً جاهزاً مهما كان الخط جيداً. لا توحّدهما.

الخطوط **مضمّنة في `assets/fonts/`** ولا تُحمّل وقت التشغيل — التطبيق يعمل
أوفلاين.

### مفردات الكروت
- عمق بتدرّج متعدد المحطات لا لون مسطّح
- `GirihPattern` علامة مائية هندسية بشفافية ~5% للكروت البطلة
- خط ذهبي شعري متلاشٍ يفصل العنوان عن القيمة
- الأيقونات داخل حلقة ذهبية دائرية لا مربّع مدوّر
- المراتب 1-3 تحصل على حلقة ميدالية (ذهبي/فضّي/برونزي)
- `SectionHeader` لكل عنوان قسم: خط نسخي + خط ذهبي ممتد

### الشعار
`MawkibLogo` — يُعرض بخلفيته البيضاء الأصلية داخل **لوحة** بحواف ناعمة وحدّ
ذهبي، فيبدو البياض عنصر تصميم لا خطأً. في التقارير المطبوعة: `plain: true`.

---

## ٦. قاعدة البيانات والصلاحيات

**المشروع:** `nmpcbyoehmghmietzurs` · المخطط في `supabase/schema.sql`
(13 جدولاً · 29 سياسة · 11 دالة).

| الجدول | الغرض |
|---|---|
| `profiles` | مرتبط بـ auth.users · `role` + `status` |
| `contributors` | المساهمون · `type` (subscriber/donor) · حقول الاشتراك · حذف ناعم |
| `payments` | كل دفعة على حدة + رقم وصل |
| `donations` | نقدي (بمبلغ) أو عيني (بوصف وكمية، **بلا مبلغ**) + إهداء لمتوفّى |
| `posts` + `post_images` | المنشورات وصور الكاروسيل + `year` للأرشيف |
| `post_likes` / `post_comments` / `post_saves` / `follows` | التفاعل |
| `story_categories` + `stories` | أقسام الستوريز يديرها المدير |
| `notifications` | تنبيهات المتأخرين |

### طبقات الوصول

| الدور | يرى |
|---|---|
| زائر (anon) | المنشورات والستوريز فقط · **يعجب** بلا حساب |
| `member` معتمد | الإحصائيات المجمّعة + المنشورات · **بلا أي اسم** · يعلّق |
| `publisher` | + النشر ورفع الستوريز |
| `finance` | + عرض وطباعة التقارير بالأسماء (**قراءة فقط**) |
| `admin` | كل شيء + الموافقات والأدوار والحظر + CRUD |

**دوال مساعدة في SQL:** `my_role()` · `is_approved()` · `is_admin()` ·
`can_read_names()` · `can_publish()` · `is_anon_session()` — كلها
`SECURITY DEFINER` لتفادي التكرار اللانهائي عند قراءة `profiles`.

**أول مدير** (مرة واحدة بعد التسجيل):
```sql
update public.profiles set role = 'admin', status = 'approved'
 where id = (select id from auth.users where email = '<البريد>');
```

**مطلوب في لوحة Supabase:** تفعيل **Anonymous sign-ins** ليعجب الزائر بلا حساب.

---

## ٧. قرارات المنتج المحسومة

مأخوذة من مقابلة 54 سؤالاً مع صاحب المشروع. **لا تُخالفها ولا تعد سؤاله عنها.**

- **المشترك والمتبرع كلاهما مانح** بفئتين، لا مستفيد ومانح.
- **لا يوجد نظام مندوبين** في هذا التطبيق.
- **المبلغ الكلي** = اشتراكات + تبرعات نقدية، والكارت يُفتح بالضغط لعرض التفصيل.
- **العملة: دينار عراقي فقط**، بفواصل آلاف وبلا كسور.
- **«نوع الاشتراك»** هي التسمية في الواجهة (شهري/سنوي) — وليس «الدورية».
- **حالة التأخير تُحسب تلقائياً** من آخر دفعة + نوع الاشتراك، ويمكن للمدير
  تجاوزها يدوياً (`is_late_override`). تسجيل دفعة جديدة يلغي التجاوز.
- **التسجيل مفتوح لكن الحساب ينتظر موافقة المدير** قبل رؤية أي رقم.
- **الرئيسية:** كارت المبلغ عريض (1×1)، تحته صف 2×1 لكارتي الأعداد، ثم قائمة
  **عمودية** بأعلى 10 متبرعين + زر «عرض الكل».
- **المنشورات:** كاروسيل صور (لا فيديو، لا منشور نصي)، ستوريز بأقسام يديرها
  المدير، أرشيف سنوات بألبوم مستقل لكل سنة.
- **التقارير:** كارتان مربعان ناعمان → جدول بعمود اسم **مثبّت** وتمرير أفقي،
  تظليل متبادل **و** لمسي، رأس بالشعار وتاريخ الطباعة، تذييل بكروت المجاميع،
  والطباعة تعرض كل الأعمدة بحجم A4.
- **أوفلاين:** قراءة وكتابة. الكتابة تُقيَّد في طابور وتُرفع تلقائياً عند عودة
  الاتصال، وقاعدة التعارض «الأحدث توقيتاً يفوز» عبر `updated_at`.
- **Realtime إلزامي:** أي نشر أو تعديل يظهر عند الجميع بلا تحديث التطبيق.
- **الحماية من الهندسة العكسية: أساسية فقط** (`--obfuscate` + إزالة السجلات).
  المستويات الأعلى تعطّل التطبيق على أجهزة كثيرة بلا مقابل حقيقي.

---

## ٨. حالة العمل

### مُنجز
واجهة الترحيب · المصادقة (تسجيل/دخول/خروج/جلسة محفوظة) · شاشة انتظار الموافقة ·
الرئيسية كاملة · صفحة المشتركين بقائمتيها مع بحث وفلترة وترتيب وحالة سداد ·
شريط التنقّل الخمسة · الوضعان الليلي والنهاري · الإعدادات الأساسية · وضع الزائر ·
التخزين المشفّر + القراءة أوفلاين مع شارة «بلا اتصال» · مخطط قاعدة البيانات
مطبَّق ومُختبَر · 14 اختبار وحدة · معاينة التصميم · APK موقّع ومشوّش.

### متبقٍ
- شاشات إضافة/تعديل المساهمين وتسجيل الدفعات والوصل القابل للطباعة
- لوحة إدارة المستخدمين (موافقات/أدوار/حظر)
- المنشورات: الفييد والكاروسيل والإعجاب والتعليق والستوريز وأرشيف السنوات
- جدول التقارير وتصدير PDF
- **طابور الكتابة أوفلاين** والمزامنة التلقائية
- **اشتراكات Realtime**
- قفل البصمة · إشعارات المتأخرين · Supabase Storage للصور
- قياس معدل الإطارات فعلياً على جهاز

### عيوب معروفة يجب إصلاحها
1. **نظاما أرقام مختلطان:** المبالغ لاتينية (`27,545,000`) والتواريخ عربية-هندية
   (`٢٠٢٦/٠٨/٠٢`) لأن intl يشتقهما من بيانات اللغة، والنتيجة تختلف بين أندرويد
   وويندوز. الحل: دالة توحيد في `Fmt` تُمرَّر عليها كل المخرجات.
2. **آخر عنصر يختفي خلف شريط التنقّل العائم** في الرئيسية وصفحة المشتركين —
   المساحة السفلية غير كافية (تحتاج ~108 بكسل).

---

## ٩. مزالق البيئة — قرأناها بالطريقة الصعبة

| المزلق | الحل |
|---|---|
| **اختلاف إصدار Flutter** | جهاز المالك على **3.38.4 / Dart 3.10.3**، وبيئة البناء على 3.44.8 / 3.12.2. قيد `environment.sdk` موسّع إلى `^3.10.0` عمداً. **لا تستخدم ميزة لغوية أحدث من Dart 3.10.** |
| **ذاكرة Gradle** | القالب الافتراضي 8G يقتل البناء على 4 غيغا. `android/gradle.properties` مضبوط على `-Xmx1536m` و `org.gradle.daemon=false`. لا ترفعه. |
| **Java لا يثق بشهادة TLS** | في بيئات تعترض HTTPS، يفشل `sdkmanager` و Gradle بينما ينجح curl. الحل: استيراد شهادة الاعتراض إلى `$JAVA_HOME/lib/security/cacerts` بـ keytool. |
| **`pkill -f GradleDaemon`** | يطابق سطر أوامر الصدفة نفسها فيقتلها قبل تنفيذ الأمر. استخدم PID مباشرة أو `pkill -f "[G]radle"`. |
| **`diff` و `strings` غير مثبّتين** | استخدم `difflib` في بايثون، و `grep -a` على الملفات الثنائية. |
| **R8 معطّل بقصد** | مكسبه ضئيل في تطبيق Flutter (معظم الحجم مكتبات أصلية) وقد يقطع الحزم التي تعتمد الانعكاس. لا تفعّله بلا اختبار على جهاز حقيقي. |
| **جهاز المالك بلا سلسلة أدوات في PATH** | لا توجّهه لأوامر طرفية. **ابنِ الـAPK وسلّمه كملف.** |
| **رفع الملفات الثنائية عبر أدوات GitHub الآلية يُفسدها** | `create_or_update_file` يرمّز المحتوى base64 بنفسه، فتمرير base64 إليه يرمّزه مرتين ويصل الملف تالفاً (خط 431KB وصل 574KB). **لا ترفع ملفاً ثنائياً بهذه الأداة.** استخدم سكربت جلب، أو اترك الرفع لـgit لدى المستخدم. |
| **GitHub Actions موقوف** | حساب المالك مقفل لمسألة فواتير: `account is locked due to a billing issue`. ليس عطلاً في الكود. |

---

## ١٠. عادات العمل

- **قبل أي commit:** `flutter analyze && flutter test` — كلاهما نظيف حالياً، أبقهما كذلك.
- **رسائل الـcommit بالعربية** بنمط `type: وصف`، ومتنها يشرح **لماذا** لا **ماذا**.
  التغييرات التي لها سبب هندسي غير بديهي تُوثَّق في المتن.
- **التعليقات تشرح القرار لا الشيفرة.** `// نزيد العداد` عديم الفائدة؛
  `// blur هنا يقتل الإطارات لأن القائمة تعيد حسابه لكل عنصر` مفيد.
- **عند اكتشاف تعارض في طلب المستخدم، أعلنه بدل تخمين النية.** حدث مراراً
  في هذا المشروع (أفقي/عمودي، طباعة/عرض داخلي، أوفلاين قراءة/كتابة) وكشفُه
  مبكراً وفّر إعادة عمل.
- **لا تدّعِ إنجازاً بلا دليل.** «بنيت الـAPK» تحتاج مسار ملف وحجماً وفحص توقيع.
  «أصلحت التصميم» تحتاج صورة معاينة منظورة.
